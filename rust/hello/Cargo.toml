[package]
name = "bench-hello"
description = "Minimal CLI for startup-time and binary-size measurements (mirror of go/hello)."
version.workspace = true
edition.workspace = true
publish.workspace = true

[[bin]]
name = "hello"
path = "src/main.rs"

[features]
alloc-stats = []
mimalloc = []
